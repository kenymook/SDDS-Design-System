# Дорожная карта — SDDS Portal и DS Builder

Документ показывает путь от готового прототипа до запуска MVP. Работа разделена на три дорожки, которые движутся согласованно: **дизайн** (что и как отрисовать), **продукт** (какие решения принять) и **инженерия** (что построить). Подробная инженерная разбивка по этапам и список эпиков — в [дорожной карте разработки](../delivery/roadmap.md); здесь — общая картина с упором на дизайн-производство и на то, от чего что зависит.

## 1. Где мы сейчас

| Что сделано | Статус |
|---|---|
| Прототип поведения (портал + DS Builder) | ✅ готов, [работающая ссылка](https://portal-flow-prototype.vercel.app) |
| Бизнес-требования и продуктовые требования | ✅ [раздел «Продукт и бизнес»](README.md) |
| Спринт-решения (авторизация, границы MVP, роли, модель проектов, градиенты) | ✅ [раздел «Решения спринта»](../deliverables/README.md) |
| Спецификация экранов (состояния и роли) | ✅ 25 из 25 экранов описаны |
| Нарезка экранов для макетирования | ✅ готова в Figma: [DS BUILDER \| PLAYGROUND](https://www.figma.com/design/3qtcAteqALdFuRL6EvR7qr/DS-BUILDER-%7C-PLAYGROUND?node-id=591-4822) |
| Финальные макеты (Figma) | 🔄 стартуют с S15: Builder в P0, Portal в MVP-объёме |
| Реализация | ⏳ не начата |

Сейчас продукт находится на первом уровне зрелости — «базовое редактирование» (цвет, градиенты, шрифты, скругления). S14 закрыл product foundation для Builder 1.0: user flow, авторизация, RBAC UI, Project / Design System / Theme и gradient support. Цель MVP — четвёртый уровень, «управление и контроль» (проекты, доступ, аудит, качество).

## 2. Что нужно решить в первую очередь

Ниже — решения, без которых нельзя закончить ни дизайн финальных макетов, ни разработку. Их важно закрыть параллельно основным дорожкам, не дожидаясь своей фазы, иначе они станут узким местом.

| Решение | За кем | Что тормозит, пока не решено |
|---|---|---|
| Провайдер авторизации (Sber ID, корпоративный вход или собственный) | Безопасность | Экран входа и весь сценарий сессии |
| Какие свойства компонентов можно менять, какие ограничены, какие закрыты | Команда системы | Редактор компонентов, проверки, модель данных |
| Как устроен CLI: откуда берёт конфигурацию, как выбирает Version, какие команды и файлы | Разработка | Передача результата в разработку, экран итога публикации |
| Как измеряется успех MVP | Продукт | Настройка аналитики и критерий приёмки |
| Какие продукты войдут в первый пилот | Продукт | Проверка модели на реальных командах |

## 3. Фазы работы

Порядок подчинён правилу «сначала фундамент, потом публикация»: модель доступа и рабочий контекст должны существовать раньше, чем появится возможность что-то опубликовать. В каждой фазе указано, что делает каждая из трёх дорожек, и когда фаза считается завершённой.

### Фаза 1. Фундамент редакторов и модель проектов
- **Дизайн:** стартовый экран Builder и выбор контекста; обзорные экраны Project, Design System и Theme; единый макет редактора с панелью свойств; состояния «унаследовано / переопределено», сравнение «старое → новое», предпросмотр, проверки, отмена изменений, gradient support.
- **Продукт:** модель Project как пользовательской границы данных и доступа зафиксирована; отдельной Organization-сущности в Builder нет.
- **Инженерия:** Project, участники и роли (Owner / Editor / Viewer), создание Design System и Theme.
- **Фаза завершена, когда:** все редакторы работают на единой модели изменений, а навигация и роли действуют.

### Фаза 2. Редактирование компонентов
- **Дизайн:** редактор компонентов; состояния «можно менять / ограничено / закрыто»; связанные с компонентом токены.
- **Продукт и команда системы:** зафиксировать перечень изменяемых, ограниченных и закрытых свойств (решение №2 из раздела выше).
- **Фаза завершена, когда:** определён разрешённый набор правок компонентов и то, как они отражаются в списке изменений.

### Фаза 3. Изменения, влияние и проверки качества
- **Дизайн:** список изменений с группировкой; какие сущности затронуты и насколько; предупреждающие проверки с готовыми исправлениями в один клик; подтверждение рисков; сводка по качеству (Health).
- **Фаза завершена, когда:** дизайнер видит различия, влияние правок и замечания, но работа при этом не блокируется.

### Фаза 4. Прямая публикация и договорённость по CLI
- **Дизайн:** экран публикации; экран результата (успех, а также ошибка с возможностью повторить); подсказка по получению результата через CLI.
- **Инженерия:** создание неизменяемой Version с описанием изменений и опубликованной конфигурации; параллельно — минимальная договорённость по CLI (решение №3).
- **Фаза завершена, когда:** дизайнер публикует изменения без согласований, а результат доступен разработчику по номеру Version.

### Фаза 5. Администрирование, аудит и качество
- **Дизайн:** настройки проекта, управление участниками и ролями, журнал действий, наблюдение за публикациями.
- **Инженерия:** управление доступом и ведение журнала.
- **Фаза завершена, когда:** Owner управляет проектом и участниками, а действия фиксируются в журнале.

### Фаза 6. История версий и откат
- **Дизайн:** сравнение версий, история конфигураций, откат через «Восстановить как черновик».
- **Фаза завершена, когда:** доступно сравнение версий и возврат к прошлой без переписывания истории.

### Параллельно — публичная часть портала
Публичная часть уже спроектирована в прототипе, но в Q3 идёт только в MVP-срезе: entry page, документация, onboarding, CTA в Builder и базовая поддержка. В финал она идёт вместе с фазами по цепочке «спецификация экрана → макет в Figma → вёрстка». Единственная зависимость от решений выше — экран входа, который упирается в выбор провайдера авторизации.

## 4. Дизайн-производство (ваша дорожка подробно)

Путь каждого экрана — от поведения в прототипе до готового макета — идёт по [инвентарю экранов](../design-spec/screen-inventory.md):

```text
Прототип        Спецификация          Макеты          Передача
(поведение)  →  (состояния, роли)  →  (Figma)      →  в разработку
    ✅               ✅ 25 из 25           🔄 S15            ⏳
```

Рекомендуемый порядок отрисовки совпадает с фазами:
1. Стартовый экран Builder и выбор контекста.
2. Обзоры Project, Design System и Theme.
3. Редакторы токенов, градиентов и панель свойств — со всеми состояниями.
4. Редактор компонентов.
5. Список изменений и состояния проверок.
6. Публикация.
7. Экран результата публикации и получение через CLI.
8. Настройки проекта и участники.
9. Экран качества (Health).
10. Версии.

Публичную часть в MVP-объёме (entry page → документация → онбординг → поддержка → вход в Builder) можно отрисовывать параллельно, но она не должна забирать capacity у Builder.

## 5. За пределами MVP

На потом (пятый уровень зрелости, «корпоративный»): согласование и ревью публикаций, наследование настроек между проектами, автоматическая доставка сгенерированных файлов, расширенная аналитика, генерация тем по текстовому описанию, полноценный откат, полный каталог всех ~80 компонентов в документации, дополнительный уровень продуктовой команды над Project — только если появится отдельное требование.

## 6. Эпики для планирования (Jira)

1. Модель проектов и дизайн-систем.
2. Роли доступа и участие в проекте.
3. Стабилизация существующих редакторов.
4. Изменения и сравнение значений.
5. Ограничения при редактировании компонентов.
6. Влияние правок и проверки.
7. Прямая публикация.
8. Интеграция с CLI.
9. Качество и аудит.
10. История версий и откат.

## 7. Ближайшие шаги

1. Закрыть решения из критического пути — провайдер авторизации, перечень изменяемых свойств компонентов и CLI contract.
2. Начать S15 как UI execution: Builder shell, Project / Design System / Theme, редактор темы с gradients, RBAC states.
3. Отрисовать Portal только в MVP-рамке: entry page, документация, onboarding, CTA в Builder.
4. Согласовать с продуктом метрики успеха MVP.
